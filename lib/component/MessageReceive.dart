import 'package:flutter/material.dart';

class MessageRecive extends StatelessWidget {
  var receive;
  MessageRecive({this.receive});
  
  @override
  Widget build(BuildContext context) {
    return Material();
  }
}
